class TeamPrincipal {
  final String name;
  final int salary;
  final int id;
  final String url;
   bool isSelected;

   TeamPrincipal(this.id, this.name, this.salary, this.url, this.isSelected  );
}
